=content_for :h1 do
  #type Catalog
  #name
    New

=form_for @catalog, :html => {:class => "fassets_core", :multipart => true} do |f|
  =render :partial => "form", :object => f
  =submit_tag "Create"
