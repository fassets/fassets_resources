-if user_signed_in?
  -unless FassetsCore::TrayPosition.where(:asset_id => @content.asset.id, :user_id => current_user.id).exists?
    =form_for [current_user, FassetsCore::TrayPosition.new(:asset_id => @content.asset.id, :user_id => current_user.id)], :url => main_app.users_tray_positions_path, :html => {:class => "inline"} do |f|
      =f.hidden_field :asset_id
      =f.hidden_field :user_id
      =submit_tag "Put on tray"
